# macOS
.DS_Store
**/.DS_Store

# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
*.egg-info/
poc/venv/
poc/.venv/

# Audio caches / generated outputs (heavy, regenerable)
poc/output*/
poc/separated/
poc/scratch/
poc/orchestrator/cache/
poc/orchestrator/outputs/

# Web build / deps
web/node_modules/
web/.next/
web/.npm-local/
web/out/
web/public/uploads/
web/public/output/
web/data/
web/tsconfig.tsbuildinfo
node_modules/
dist/
build/
coverage/
.netlify
.next/

# Working / generated wav files (regenerable)
poc/vocals_old.wav
poc/*.wav

# Old demo outputs (regenerable)
rebelzai_personalized_*.mp3

# Render inputs/outputs staged into the workspace at runtime (do not commit)
King_of_the_North.mp3
RoarBliss_Clarence_CLOUD.mp3

# Editor / IDE
.antigravitycli/
.vscode/
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
*.pem

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
!**/SPRINT_*_REPORT.md

# Secrets (NEVER COMMIT)
.env
.env.*
!.env.example

# Local notebooks / scratch
*.ipynb_checkpoints/

# Excalidraw logs / wordpress logs
excalidraw.log
wordpress-mcp.log

# eval harness test artifacts
eval/_out_*.mp3
eval/last_results.json

# Local audio renders + listening/reference sets (heavy binaries, regenerable — kept locally, never committed).
# A handful of approved references stay on disk (e.g. RoarBliss_GoT_v6_2min.mp3, the c482a0a4 takes) but are
# ignored here so they don't clutter `git status`.
/*.mp3
/RoarBliss_*/
/reference_demos/
/OMNIVOICE_REFERENCE/

# Local handoff docs, runbooks, website source images, throwaway screenshots (kept on disk, not in git)
/HANDOFF_*.md
/GO_LIVE_RUNBOOK.pdf
/Background images for website app/
/*.png

# Local cloud-test logs + secondary build dir
/web/cloudtest_*.txt
/web/.next-build/

# Browser-automation cache
/.playwright-mcp/
