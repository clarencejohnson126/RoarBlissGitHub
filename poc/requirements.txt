# Roar Bliss orchestrator dependencies — minimal set for Docker builds
# Pinned to versions known to work together. CPU build of PyTorch is fine
# for our scale (Demucs and Whisper run fast enough on CPU on Render's
# Standard plan; TTS happens on Replicate GPU).

# ── Audio processing core ──────────────────────────────────────────────
pydub==0.25.1
librosa==0.10.2
numpy<2.3
soundfile==0.13.1
audioread==3.0.1
scipy>=1.11

# ── PyTorch (CPU build for Demucs / Whisper / pyannote) ────────────────
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.5.1
torchaudio==2.5.1

# ── Source separation ──────────────────────────────────────────────────
demucs==4.0.1

# ── Transcription ──────────────────────────────────────────────────────
openai-whisper==20240930

# ── Speaker diarization ────────────────────────────────────────────────
pyannote.audio==4.0.4

# ── LLM clients ────────────────────────────────────────────────────────
anthropic==0.75.0      # Claude Haiku 4.5 — planner stages
ollama==0.6.2          # only used if local Ollama is reachable (skipped in cloud)

# ── HTTP + utilities ───────────────────────────────────────────────────
requests==2.32.3
